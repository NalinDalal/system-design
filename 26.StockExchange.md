## Stock Exchange

### 1. Problem Scope & Requirements
- **Trade only stocks** (not options/futures)
- Support placing/canceling orders, limit orders
- No after-hours trading
- Scale: billions of orders/day, 100+ symbols, 100K+ users
- Simple risk checks (e.g., max shares per user/day)
- Wallet management: ensure sufficient funds

#### Non-functional Requirements
- Availability: 99.99%+
- Fault tolerance & fast recovery
- Millisecond latency (focus on 99th percentile)
- Security: KYC, DDoS protection

#### Back-of-the-envelope Estimation
- 100 symbols, 1B orders/day, QPS ≈ 43,000 (peak ≈ 215,000)

---

### 2. High-Level Architecture

#### Mermaid Diagram: Stock Exchange High-Level Design
```mermaid
graph TD
	Broker[Broker: Robinhood, Goldman Sachs, etc] --> ClientGateway[Client Gateway]
	ClientGateway --> OrderManager[Order Manager]
	OrderManager --> Sequencer[Sequencer]
	Sequencer --> MatchingEngine[Matching Engine]
	MatchingEngine --> OrderBook[Order Book]
	MatchingEngine --> Reporter[Reporter]
	Reporter --> DB[DB]
	MatchingEngine --> MarketDataPublisher[Market Data Publisher]
	MarketDataPublisher --> DataService[Data Service]
	MarketDataPublisher --> CandlestickChart[Candlestick Chart]
	MarketDataPublisher --> OrderBook
	DataService --> Broker
	Reporter --> AggregatedRiskCheck[Aggregated Risk Check]
	OrderManager --> Wallet[Wallet]
	OrderManager --> AggregatedRiskCheck
	OrderManager --> Reporter
	Reporter --> DB
	DB --> orders[orders, executions]
	MarketDataPublisher --> MarketDataFlow[Market Data Flow]
	MarketDataFlow --> Broker
	MarketDataFlow --> DataService
	MarketDataFlow --> MarketDataPublisher
```

---

### 3. Trading Flow (Sequence)

```mermaid
sequenceDiagram
	participant Client as Client
	participant Broker as Broker
	participant Gateway as Client Gateway
	participant OrderMgr as Order Manager
	participant Sequencer as Sequencer
	participant MatchEng as Matching Engine
	participant Reporter as Reporter
	participant MDP as Market Data Publisher
	participant DataSvc as Data Service

	Client->>Broker: Place order
	Broker->>Gateway: Send order
	Gateway->>OrderMgr: Validate, rate limit, normalize
	OrderMgr->>Sequencer: Risk check, wallet check
	Sequencer->>MatchEng: Stamp sequence ID, send order
	MatchEng->>OrderMgr: Return executions
	OrderMgr->>Broker: Return executions
	MatchEng->>MDP: Send executions
	MDP->>DataSvc: Publish market data
	Reporter->>DB: Store reporting data
```

---

### 4. Data Models

```mermaid
classDiagram
	class Order {
		orderID: UUID
		productID: int
		price: long
		quantity: long
		side: Side
		orderStatus: OrderStatus
		orderType: OrderType
		timeInForce: TimeInForce
		symbol: long
		userID: long
		clientOrderID: string
		broker: string
		accountID: long
		entryTime: long
		transactionTime: long
	}
	class Execution {
		execID: UUID
		orderID: UUID
		price: long
		quantity: long
		side: Side
		orderStatus: OrderStatus
		orderType: OrderType
		symbol: long
		userID: long
		feeCurrency: Currency
		feeRate: long
		feeAmount: long
		execStatus: ExecStatus
		transactionTime: long
	}
	class Product {
		productID: int
		symbol: type
		lotSize: int
		tickSize: decimal
		quoteCurrency: Currency
		settleCurrency: Currency
		description: string
		field: type
	}
	Order "1" -- "0..n" Execution
	Order "1" -- "1" Product
	Execution "1" -- "1" Product
```

---

### 5. Order Book Structure & Operations

Efficient order book must support:
- Constant lookup time
- O(1) add/cancel/match operations
- Fast update/query best bid/ask
- Iterate price levels

```mermaid
graph TD
  subgraph OrderBook
    PriceLevel1[Price: 100.13 - Qty: 100, 200]
    PriceLevel2[Price: 100.12 - Qty: 600, 900]
    PriceLevel3[Price: 100.11 - Qty: 900, 700, 400]
    PriceLevel4[Price: 100.10 - Qty: 200, 400, 1100, 100]
    PriceLevel1 --> PriceLevel2 --> PriceLevel3 --> PriceLevel4
  end
  subgraph BuyBook
    PriceLevelB1[Price: 100.08 - Qty: 500, 600, 900]
    PriceLevelB2[Price: 100.07 - Qty: 100, 700]
    PriceLevelB3[Price: 100.06 - Qty: 100, 400, 300, 200]
    PriceLevelB4[Price: 100.05 - Qty: 500, 100]
    PriceLevelB1 --> PriceLevelB2 --> PriceLevelB3 --> PriceLevelB4
  end
  OrderBook --> BuyBook
```

---

### 6. Performance, Event Sourcing, High Availability

- Latency: minimize tasks on critical path, reduce network/disk access
- Use mmap for event store, single-threaded application loops pinned to CPU
- Event sourcing: store immutable log of state changes
- High availability: hot-warm replicas, Raft for leader election, fast failover

```mermaid
graph TD
	OrderManager --> Sequencer
	Sequencer --> EventStore[mmap Event Store]
	EventStore --> MatchingEngine
	MatchingEngine --> MarketDataPublisher
	MarketDataPublisher --> DataService
	Reporter --> EventStore
	EventStore --> Reporter
```

---

### 7. Security & Fairness
- Isolate public/private services
- Use caching, harden URLs, safelist/blocklist, rate limiting
- Colocation for VIP clients, multicast for market data fairness